# syncro.co.il

Static website for Syncro (a TalGuy Group company). Currently serving the "under maintenance" holding page.

- `index.html` — holding page (RTL Hebrew, Syncro design tokens, animated hero)
- `assets/syncro-building.js` — canvas web component `<syncro-building progress="0..1">` used by the hero loop
- `CNAME` — custom domain for GitHub Pages
